-- ============================================
-- Admin Ban Script (English)
-- Place in: ServerScriptService > Script
-- ============================================

local Players = game:GetService("Players")

-- Add your UserId here
local ADMINS = {
    123456789,  -- Replace with your UserId
}

local bannedPlayers = {}

local function isAdmin(player)
    for _, id in ipairs(ADMINS) do
        if player.UserId == id then return true end
    end
    return false
end

Players.PlayerAdded:Connect(function(player)

    -- Kick banned players on join
    if bannedPlayers[player.UserId] then
        player:Kick("You are banned from this server.")
        return
    end

    -- Listen for chat commands
    player.Chatted:Connect(function(message)
        if not isAdmin(player) then return end

        local args = message:split(" ")
        local cmd = args[1]
        local targetName = args[2]

        -- Ban one player
        if cmd == "!ban" and targetName then
            local target = Players:FindFirstChild(targetName)
            if target then
                bannedPlayers[target.UserId] = true
                target:Kick("You have been banned by an admin.")
                print("[ADMIN] " .. player.Name .. " banned " .. target.Name)
            else
                print("[ADMIN] Player not found: " .. targetName)
            end

        -- Kick one player
        elseif cmd == "!kick" and targetName then
            local target = Players:FindFirstChild(targetName)
            if target then
                target:Kick("You have been kicked by an admin.")
                print("[ADMIN] " .. player.Name .. " kicked " .. target.Name)
            end

        -- Ban all players
        elseif cmd == "!banall" then
            for _, target in ipairs(Players:GetPlayers()) do
